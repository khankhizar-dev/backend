$kind: graphql-request
name: Get My Preferences
order: 2000
url: '{{baseUrl}}/graphql'
query: |-
  query MyPreferences {
    myPreferences {
      currency
      language
      dateFormat
      units
      theme
      timezone
    }
  }
variables: '{}'
